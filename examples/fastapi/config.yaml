services:
  static:
    component:
      type: fastapi
      app: static:application

  dynamic:
    component:
      type: fastapi
      components:
        myroot:
          type: dynamic:WebRootComponent
